# حالة إصدار TAZA 041 الأكاديمي — 2026-08-21

هذه هي النسخة الأكاديمية النهائية المعتمدة لمشروع الويب ولوحة الموظفين. اكتمل
التنظيف والفحص ولا توجد أعمال برمجية إلزامية معلقة ضمن نطاق العرض والتقييم المحلي.

## نطاق المشروع

| المكوّن | المسار المحلي المعتمد |
| --- | --- |
| Laravel والموقع والداشبورد | `C:\Users\DELL005\Desktop\TAZA041 - Web & Dashboard` |
| تطبيق الزبون Flutter | `C:\Users\DELL005\Desktop\taza041_flutter_customer_mobile` |
| المواد المؤرشفة | `C:\Users\DELL005\Desktop\Archive\TAZA041-Web-2026-08-13` |

يبقى تطبيق Flutter مشروعًا مستقلًا بجانب Laravel. لا تُنشأ نسخة منه داخل مجلد
الويب أو `public`.

## نتيجة الفحص النهائي

- نجح `composer check`: تنسيق PHP سليم، ونجح 112 اختبارًا مع 1303 عمليات تحقق.
- نجح فحص صياغة ملفات PHP المتغيرة والجديدة وجميع ملفات JavaScript الـ68 داخل `public`.
- جميع ترحيلات قاعدة البيانات المحلية مطبقة، بما فيها إعدادات مضاعفات الولاء.
- يعمل رابط `public/storage`، ونجح تخزين إعدادات Laravel ومساراته مؤقتًا.
- يعرض Laravel حاليًا 194 مسارًا خارج مسارات الحزم.
- نجح تدقيق Composer الأمني بعد تحديث Guzzle إلى `7.15.3` وCommonMark إلى
  `2.10.0`، ولا توجد نشرات أمنية معروفة في ملف القفل.
- حزمتا PHPUnit المهجورتان الظاهرتان في التدقيق تبعيتان لأدوات الاختبار فقط، ولا
  توجد لهما بدائل مقترحة أو نشرات أمنية؛ إبقاؤهما ضروري لمجموعة الاختبارات الحالية.
- آخر تحقق موثق لتطبيق Flutter: تحليل ساكن بلا أخطاء أو تحذيرات، و30 اختبارًا
  ناجحًا، وبناء APK Debug ناجح.

## ما نُظف وأُرشف

- نُقلت مصادر الرسوم، ملفات PDF الناتجة، اختصارات التشغيل المكررة، مراجعات Word،
  النسخة القديمة الكاملة، وملفات العمل المؤقتة إلى مجلد الأرشيف أعلاه.
- نُقلت ثلاثة أصول عامة غير مستخدمة إلى الأرشيف بعد التحقق من عدم وجود مراجع لها.
- حُفظت نسخة من قاعدة SQLite قبل تطبيق آخر ترحيلين.
- حُفظت أيضًا صورتا Use Case اللتان كانتا محذوفتين قبل بدء التنظيف، باستعادتهما من
  Git إلى الأرشيف فقط.
- أزيلت السجلات والكاشات ونتائج الاختبارات القابلة لإعادة الإنشاء.
- يحتوي الأرشيف على ملفات بيان توضّح المواد المنقولة ومكانها.

## التشغيل الأكاديمي

من جذر المشروع:

```bat
open-index.bat
```

أو شغّل `start-backend.bat` و`start-frontend.bat` كلًا في نافذة مستقلة. عناوين
العرض الافتراضية:

- موقع الزبون: `http://127.0.0.1:5500/frontend/`
- لوحة الموظفين: `http://127.0.0.1:5500/dashboard/`
- Laravel API: `http://127.0.0.1:8000/api`
- فحص الجاهزية: `http://127.0.0.1:8000/api/health`

حساب المدير العام هو `admin` وكلمة المرور الأكاديمية الابتدائية `Admin@041`.
بقية حسابات الأدوار موثقة في `README.md`.

## حدود النسخة الأكاديمية

- الدفع النقدي ونقاط الولاء داخل نطاق العرض. سيريتل كاش وشام كاش معطلتان عمدًا
  لأن المشروع لا يتصل ببوابات دفع تجارية.
- المساعد الذكي حتمي ويستخدم تصنيف النوايا وبيانات القائمة المحلية، ولا يعتمد على
  خدمة ذكاء اصطناعي خارجية.
- خدمة OSRM العامة ملائمة للعرض الأكاديمي، وقد يستخدم النظام تقديرًا احتياطيًا عند
  تعذر الاتصال بها.
- لا تتطلب النسخة الأكاديمية نطاقًا عامًا أو HTTPS أو بريد SMTP أو متجر تطبيقات أو
  مفاتيح توقيع تجارية.

## إعادة التحقق

شغّل قبل التسليم أو بعد أي تعديل لاحق:

```bat
composer check
composer audit --locked
php artisan migrate:status
```

الحالة النهائية: **جاهز للتسليم والعرض الأكاديمي**.
